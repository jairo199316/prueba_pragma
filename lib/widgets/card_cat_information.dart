import 'package:flutter/material.dart';

class CardCatInformation extends StatelessWidget {
  const CardCatInformation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
